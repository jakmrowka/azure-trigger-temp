replicaCount: 1

image:
  repository: "#{dockerhub-user}#/#{image_name}#"
  pullPolicy: Always
  tag: "latest"

service:
  type: NodePort
  port: 30003
  nodePort: 30003

ingress:
  enabled: false

config:
  TUYA_ACCESS_ID: "#{tuya-access-id}#"
  TUYA_ACCESS_KEY: "#{tuya-access-key}#"
  API_KEY: "#{api_key}#"

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
